{
  "name": "ipc-ts",
  "version": "1.0.0",
  "description": "ipc通信のテスト(ts)",
  "main": "dist/main.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.2.1",
    "typescript": "^4.1.3"
  }
}
